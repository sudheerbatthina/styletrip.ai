export function getOpenAiProvider() {
  // TODO: Wire OpenAI text/image calls here after provider selection, consent,
  // cost confirmation, and paid-generation limits are fully enabled.
  throw new Error("OpenAI provider is not enabled yet.");
}