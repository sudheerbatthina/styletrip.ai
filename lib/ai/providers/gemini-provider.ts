export function getGeminiProvider() {
  // TODO: Wire Gemini calls here after provider selection, consent,
  // cost confirmation, and paid-generation limits are fully enabled.
  throw new Error("Gemini provider is not enabled yet.");
}